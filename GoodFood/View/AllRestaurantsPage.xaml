<UserControl x:Class="GoodFood.View.AllRestaurantsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GoodFood.View"
            xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
         TextElement.Foreground="{DynamicResource MaterialDesignBody}"
         TextElement.FontWeight="Regular"
         TextElement.FontSize="13"
         TextOptions.TextFormattingMode="Ideal"
         TextOptions.TextRenderingMode="Auto"
         FontFamily="{DynamicResource MaterialDesignFont}"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="Transparent">
        <ListBox Margin="15,0,0,0" ItemsSource="{Binding Restaurants}" SelectedItem="{Binding SelectedRestaurant}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel IsItemsHost="True" Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel>
                        <Image DockPanel.Dock="Left" Width="100" Height="100" Stretch="Fill" Source="{Binding Path=Image}"/>
                        <StackPanel Margin="20,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Ресторан "/>
                                <TextBlock Text="{Binding Path=Name}"/>
                            </StackPanel>
                            <TextBlock Text="{Binding Path=Type_of_cuisine}"/>
                            <TextBlock Text="{Binding Path=Number_of_tables}"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Path=Start_time}"/>
                                <TextBlock Text="-"/>
                                <TextBlock Text="{Binding Path=End_time}"/>
                            </StackPanel>
                        </StackPanel>
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
